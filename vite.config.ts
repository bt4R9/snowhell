import { defineConfig } from 'vite';

export default defineConfig({
  /**
   * ★ ПРОЕКТ ЛЕЖИТ НЕ В КОРНЕ ДОМЕНА. GitHub Pages отдаёт его по адресу
   * /snowhell/, и со стандартным base = '/' все ссылки на бандл и ассеты
   * ушли бы в корень домена — страница открылась бы пустой.
   */
  base: '/snowhell/',
  build: {
    /**
     * ★ ЦЕЛЬ ВЫШЕ es2020 РАДИ TOP-LEVEL AWAIT. main.ts грузит игру и отладочные
     * модули через await на верхнем уровне — это осознанно: сид задаётся ДО
     * создания мира, и порядок важнее удобства сборки. На es2020 такой синтаксис
     * запрещён, и сборка падала; браузеры, которые нас интересуют, top-level
     * await поддерживают с 2021 года.
     */
    target: 'es2022',
  },
});
